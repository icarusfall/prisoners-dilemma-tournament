// @pdt/frontend — Vite + vanilla TS + Mapbox (later phases).
// Real skeleton lands in Phase 1 task 12.
import { ENGINE_VERSION } from '@pdt/engine';

const app = document.getElementById('app');
if (app) {
  app.textContent = `@pdt/frontend: not yet implemented (engine v${ENGINE_VERSION})`;
}
