// @pdt/backend — Fastify server, Postgres persistence, MCP server (later phases).
// Real skeleton lands in Phase 1 task 9.
import { ENGINE_VERSION } from '@pdt/engine';

console.log(`@pdt/backend: not yet implemented (engine v${ENGINE_VERSION})`);
